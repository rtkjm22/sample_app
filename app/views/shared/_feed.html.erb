<% if @feed_items.any? %> 
  <ol class="microposts">
    <%= render @feed_items %>
  </ol><!-- / .microposts -->
  <%= will_paginate @feed_items, params: { controller: :static_pages, action: :home } %>
<% end %>